'use strict';

import d3 from 'd3';

import Classes from '../../constants/CommonSVGClasses';

import MetadataActions from '../../actions/MetadataActions';

import REST from '../../utils/ServiceMethods';

class EditToI {

  // static startEdit(data) {
  //   var vertices = JSON.parse(data.polygonVertices);
  //   // Select ToI container, append vertices for edit
  //   var editor = d3.select(d3.select('#PATH-' + data.uid).node().parentNode)
  //     .append('g')
  //     .attr('class', 'TOI-EDITOR');
  //
  //   for(var i = 0; i < vertices.length; ++i) {
  //     var vertex = vertices[i];
  //     editor.append()
  //   }
  // }
}

export default EditToI
